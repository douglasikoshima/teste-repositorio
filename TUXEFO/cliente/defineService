#DEFINE SERVICO
FWDIR=$(TUXDIR_DES)
PCEXT=pcpp
TUXINCDIR=$(TUXDIR)/include
#export CFLAGS=-L$(ORACLE_HOME)/lib32 -L/opt/esac/dvfo01rt/staging/lib -L/opt/tuxedo/current/lib -L/usr/lib -L/usr/local/lib -L$(FWDIR)/lib
export CFLAGS=-L$(ORALIBDIR) -L/opt/esac/dvfo01rt/staging/lib -L/opt/tuxedo/current/lib -L/usr/lib -L/usr/local/lib -L$(FWDIR)/lib
export CC='xlC -qrtti'
ORAINCDIR=-I$(ORACLE_HOME)/precomp/public
#ORALIBDIR=-L$(ORACLE_HOME)/lib32
XERCES_CLIB=-lxerces-c
AUXOBJ=$(CUSTOBJ)
FWLIB=-ltuxfw
CC=$(SACCPP) $(PINCLUDE) $(PLIBPATH) -G
SRC2OBJ=$(CC) -c -g
OBJ2BIN=$(CC) -g -o
PROC=proc include=$(TUXINCDIR) include=$(TUXDIR_DES)/include/ClienteCommons code=cpp cpp_suffix=cpp parse=none
SRC=$(CPP) $(PC:.$(PCEXT)=.cpp)
OBJS=lib/*.o
INCDIRS=-I$(TUXINCDIR) -I$(FWDIR)/include -Iinclude -I$(TUXDIR_DES)/include/clienteCmm

#DEFINE SERVICO
FWDIR=$(TUXDIR_DES)
PCEXT=pcpp
TUXINCDIR=$(TUXDIR)/include
export CFLAGS=-L$(ORALIBDIR) -L$(TUXDIR_DES)/lib
export CC='xlC -qrtti'
ORAINCDIR=-I$(ORACLE_HOME)/precomp/public
XERCES_CLIB=-lxerces-c
AUXOBJ=$(CUSTOBJ)
FWLIB=-ltuxfw
CC=$(SACCPP) $(PINCLUDE) $(PLIBPATH) -G
SRC2OBJ=$(CC) -c -g
OBJ2BIN=$(CC) -g -o
PROC=proc include=$(TUXINCDIR) include=include include=$(TUXDIR_DES)/include/InterfaceCommons code=cpp cpp_suffix=cpp parse=none
SRC=$(CPP) $(PC:.$(PCEXT)=.cpp)
OBJS=lib/*.o
INCDIRS=-I$(TUXINCDIR) -I$(FWDIR)/include -Iinclude -I$(TUXDIR_DES)/include/InterfaceCommons
